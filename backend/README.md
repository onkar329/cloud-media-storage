# Backend

## Stack
Node.js, Express, TypeScript, Supabase/PostgreSQL, JWT.

## Setup
```bash
npm install
cp .env.example .env
npm run dev
```

The API runs on port 8080.

## Supabase
Create a private Storage bucket named `drive`, then apply `sql/schema.sql`.
